import { expect } from 'chai';
import { polyfills } from '../index';

describe('Config tests', () => {
    it('should not allow to mutate polyfills object', () => {
        expect(true).to.equal(true);
    });
});
